<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1" name="viewport" />
    <title>ESP252 • Brain with In-SVG Backgrounds</title>
    <style>
      :root {
        --bg: #0f1216;
        --ink: #e7edf3;
        --muted: #9fb3c8;
        --accent-1: #7ec8ff;
        --accent-2: #a7ffcf;
        --grid: #1b222b;
        --card: #0b121c;
      }
      * {
        box-sizing: border-box;
      }
      body {
        margin: 0;
        font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial;
        background: radial-gradient(1200px 800px at 80% 30%, #111725 0%, #0c0f14 60%), var(--bg);
        color: var(--ink);
        min-height: 100vh;
      }
      header {
        padding: 16px 24px;
        border-bottom: 1px solid var(--grid);
      }
      .container {
        max-width: 1100px;
        margin: 0 auto;
        padding: 0 24px;
      }
      .overview {
        padding: 22px 0 0 0;
        display: grid;
        gap: 12px;
      }
      .kicker {
        color: var(--muted);
        letter-spacing: 0.14em;
        text-transform: uppercase;
        font-size: 12px;
      }
      h1 {
        margin: 0;
        font-size: clamp(28px, 4.4vw, 48px);
        line-height: 1.1;
      }
      .sub {
        color: var(--muted);
        max-width: 70ch;
      }
      .legend {
        display: flex;
        gap: 14px;
        align-items: center;
        margin-top: 6px;
      }
      .chip {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        padding: 8px 12px;
        background: #0f172a;
        border: 1px solid var(--grid);
        border-radius: 999px;
        font-size: 12px;
        color: var(--muted);
      }
      .chip a {
        color: inherit;
        text-decoration: none;
      }
      .dot {
        width: 10px;
        height: 10px;
        border-radius: 50%;
        display: inline-block;
      }
      .dot.c {
        background: var(--accent-1);
        box-shadow: 0 0 20px #7ec8ff33;
      }
      .dot.l {
        background: var(--accent-2);
        box-shadow: 0 0 20px #a7ffcf33;
      }

      .wrap {
        position: relative;
        max-width: 1100px;
        margin: 18px auto 26px;
        padding: 0 24px;
      }
      .card {
        position: relative;
        background: linear-gradient(145deg, #0e1624, var(--card));
        border: 1px solid var(--grid);
        border-radius: 22px;
        min-height: 60vh;
        overflow: hidden;
        box-shadow: 0 10px 28px rgba(0, 0, 0, 0.45), inset 0 1px 0 rgba(255, 255, 255, 0.06);
      }
      .title {
        position: absolute;
        top: 14px;
        left: 18px;
        color: var(--muted);
        letter-spacing: 0.14em;
        text-transform: uppercase;
        font-size: 12px;
      }

      .brain {
        width: min(820px, 92vw);
        display: block;
        margin: 38px auto 0;
        filter: drop-shadow(0 10px 30px rgba(0, 0, 0, 0.35));
      }

      .labels {
        position: absolute;
        inset: 0;
        pointer-events: none;
        z-index: 7;
      }
      .tag {
        position: absolute;
        display: inline-flex;
        align-items: center;
        gap: 8px;
        padding: 12px 16px;
        background: linear-gradient(180deg, rgba(10, 16, 26, 0.92), rgba(10, 16, 26, 0.85));
        border: 1px solid #263447;
        border-radius: 999px;
        font-weight: 800;
        letter-spacing: 0.06em;
        text-transform: uppercase;
        text-shadow: 0 1px 0 rgba(0, 0, 0, 0.6);
        box-shadow: 0 6px 18px rgba(0, 0, 0, 0.45), 0 0 0 2px rgba(126, 200, 255, 0.1);
        pointer-events: auto;
      }
      .tag a {
        color: inherit;
        text-decoration: none;
      }
      .tag .dot {
        width: 10px;
        height: 10px;
        border-radius: 50%;
        display: inline-block;
      }
      .tag.cre {
        color: #d6efff;
      }
      .tag.lead {
        color: #eafff6;
      }
      .tag.cre .dot {
        background: var(--accent-1);
        box-shadow: 0 0 24px #7ec8ff77;
      }
      .tag.lead .dot {
        background: var(--accent-2);
        box-shadow: 0 0 24px #a7ffcf77;
      }
      .tag.cre {
        left: 28%;
        top: 30%;
        transform: translate(-50%, -50%);
      }
      .tag.lead {
        left: 70%;
        top: 30%;
        transform: translate(-50%, -50%);
      }

      .half {
        position: absolute;
        top: 0;
        bottom: 0;
        width: 50%;
        z-index: 8;
        text-decoration: none;
      }
      .half.cre {
        left: 0;
      }
      .half.lead {
        right: 0;
      }

      footer {
        padding: 14px 0 40px;
        color: #65748a;
        font-size: 13px;
      }
    </style>
  </head>
  <body>
    <header class="container">ESP252 • Unlocking My Right Brain</header>
    <section aria-labelledby="hero-title" class="container overview">
      <div class="kicker">Reflection</div>
      <h1 id="hero-title">Unlocking the Right Side of My Brain</h1>
      <p class="sub">
        Kids are wildly creative—then school, routines, and fear of being wrong can quiet that voice.
        This course felt like turning that volume back up: practicing curiosity, taking playful risks,
        and rebuilding confidence in the right hemisphere. The map below shows how Creativity and
        Leadership reconnected for me over the semester.
      </p>
      <div class="legend" role="list">
        <span class="chip" role="listitem">
          <span class="dot c"></span>
          <a href="creativity.html">Creativity</a>
        </span>
        <span class="chip" role="listitem">
          <span class="dot l"></span>
          <a href="leadership.html">Leadership</a>
        </span>
      </div>
    </section>

    <div class="wrap">
      <div class="card">
        <div class="title">Right Hemisphere</div>

        <!-- your SVG brain here; keeping a placeholder rect so layout stays intact -->
        <svg
          aria-labelledby="brainLabel"
          class="brain"
          role="img"
          viewBox="0 0 520 420"
          xmlns="http://www.w3.org/2000/svg"
        >
          <title id="brainLabel">
            Right Hemisphere with internal backgrounds, Creativity left &amp; Leadership right
          </title>
          <!-- replace this rect with your real SVG from your original file -->
          <rect
            x="10"
            y="10"
            width="500"
            height="400"
            rx="120"
            ry="120"
            fill="rgba(10,16,26,.15)"
            stroke="rgba(231,237,243,.12)"
          />
        </svg>

        <div class="labels">
          <div class="tag cre">
            <span class="dot"></span>
            <a aria-label="Open Creativity toolbelt" href="creativity.html">Creativity</a>
          </div>
          <div class="tag lead">
            <span class="dot"></span>
            <a aria-label="Open Leadership toolbelt" href="leadership.html">Leadership</a>
          </div>
        </div>

        <a aria-label="Open Creativity toolbelt" class="half cre" href="creativity.html"></a>
        <a aria-label="Open Leadership toolbelt" class="half lead" href="leadership.html"></a>
      </div>
    </div>

    <footer class="container">Bennett Geissler · Fall 2025</footer>
  </body>
</html>
